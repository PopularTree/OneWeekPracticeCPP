#include <iostream>

using namespace std;

int main(void)
{
	int first, second;

	cout << "first number:";
	cin >> first;
	cout << "second number:";
	cin >> second;
	cout << first << " + " << second << " = " << first + second << endl;
		cout << first << " - " << second << " = " << first - second << endl;
	return (0);
}